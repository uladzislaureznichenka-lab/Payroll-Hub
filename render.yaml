services:
  - type: web
    name: payroll-hub-api
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: python -m gunicorn -w 4 -b 0.0.0.0:$PORT "app:create_app()"
